# experiments

This folder contains some experiments, mainly to see how different ways
of linking a binary, and structuring the library files, affects the
overall file size.

The primary goal here is to make the binaries as small as possible, after
all.
